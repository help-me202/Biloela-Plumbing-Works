{
  "buildCommand": "npm run build",
  "outputDirectory": "public",
  "routes": [
    { "src": "/HTML/(.*)", "dest": "/$1" },
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
